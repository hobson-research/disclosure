$(document).ready(function(){
	
	//	*	*	*	*	*	*	*	*	*	*
	// Misc
	//	*	*	*	*	*	*	*	*	*	*
	
	// Select all content in textarea for easier copy & paste
	$('.selectAll').click(function () {
		$(this).select();
	});

	// $('#timeCount').stopwatch().stopwatch('start');

});